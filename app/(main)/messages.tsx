import { Card } from "@/components/Card";
import { Text } from "react-native";

export default function Index() {
  return (
    <Card>
      <Text style={{
        color: "white"
      }}>Messages</Text>
    </Card>
  );
}
